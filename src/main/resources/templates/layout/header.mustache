<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>코드요</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700;900&display=swap" rel="stylesheet"/>
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        :root {
            --navy:      #0d1b2e;
            --cyan:      #00c8e0;
            --cyan-dark: #00a8be;
            --blue:      #2563eb;
            --blue-dark: #1d4ed8;
            --blue-soft: #eff6ff;
            --white:     #ffffff;
            --gray:      #6b7280;
            --gray-lt:   #9ca3af;
            --border:    #e5e7eb;
        }

        body {
            font-family: 'Noto Sans KR', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            padding-top: 64px;
        }

        a { text-decoration: none; color: inherit; }

        /* ══ NAV ══ */
        .nav {
            position: fixed !important;
            top: 0 !important; left: 0 !important; right: 0 !important;
            width: 100% !important;
            z-index: 99999 !important;
            background: #fff;
            border-bottom: 1px solid var(--border);
            height: 64px;
            display: flex !important;
            align-items: center;
            box-shadow: 0 2px 12px rgba(0,0,0,.08);
        }
        .nav-inner {
            width: 100%;
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 28px;
            height: 64px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 16px;
        }
        .nav-logo {
            display: flex; align-items: center; flex-shrink: 0;
        }
        .nav-logo img { display: block; height: 28px; width: auto; }

        /* 일반 링크 */
        .nav-links {
            display: flex; gap: 24px; align-items: center;
            font-size: 14px; color: #6b7280; font-weight: 500;
        }
        .nav-links a:hover { color: #1c1e26; }

        /* 전문가 탭 네비 */
        .nav-links.expert-links {
            flex: 1; gap: 0; justify-content: center;
            overflow-x: auto; scrollbar-width: none;
        }
        .nav-links.expert-links::-webkit-scrollbar { display: none; }
        .expert-nav-tab {
            padding: 0 18px; height: 64px;
            display: flex; align-items: center;
            font-size: 13px; font-weight: 600; color: #6b7280;
            border-bottom: 2px solid transparent; white-space: nowrap;
            transition: color .15s, border-color .15s;
        }
        .expert-nav-tab:hover { color: #1c1e26; border-color: #d1d5db; }
        .expert-nav-tab.active { color: var(--blue); border-color: var(--blue); }

        /* 우측 액션 */
        .nav-actions {
            display: flex; align-items: center; gap: 8px;
            flex-shrink: 0; white-space: nowrap;
        }

        /* 버튼 */
        .btn {
            display: inline-block; padding: 9px 22px; border-radius: 25px;
            font-size: 14px; font-weight: 700; cursor: pointer;
            text-decoration: none; transition: all .15s; border: none;
            font-family: inherit;
        }
        .btn-outline {
            border: 1.5px solid var(--border); color: #1e293b; background: #fff;
        }
        .btn-outline:hover { border-color: var(--blue); color: var(--blue); }
        .btn-primary { background: var(--blue); color: #fff; }
        .btn-primary:hover { background: var(--blue-dark); }

        /* 잔액 위젯 */
        .nav-balance-wrap {
            display: flex; align-items: center; gap: 6px;
            background: #eff6ff; border: 1.5px solid #bfdbfe;
            border-radius: 10px; padding: 6px 12px;
            flex-shrink: 0; white-space: nowrap;
        }
        .nav-balance-label { font-size: 11px; font-weight: 600; color: #3b82f6; }
        .nav-balance-amount { font-size: 14px; font-weight: 900; color: #1d4ed8; }
        .nav-charge-btn {
            font-size: 11px; font-weight: 700;
            background: var(--blue); color: #fff;
            border-radius: 6px; padding: 3px 9px;
            text-decoration: none; transition: background .15s;
        }
        .nav-charge-btn:hover { background: var(--blue-dark); }

        /* 알림 벨 */
        .nav-bell {
            position: relative; display: inline-flex;
            align-items: center; justify-content: center;
            width: 40px; height: 40px; border-radius: 10px;
            color: #6b7280; transition: background .15s, color .15s;
        }
        .nav-bell:hover { background: #eff6ff; color: var(--blue); }
        .nav-bell svg { width: 21px; height: 21px; }
        .nav-bell-dot {
            position: absolute; top: 8px; right: 9px;
            width: 9px; height: 9px;
            background: #22c55e; border: 2px solid #fff; border-radius: 50%;
        }

        /* 프로필 드롭다운 */
        .profile-menu { position: relative; }
        .profile-trigger {
            background: none; border: none; cursor: pointer;
            padding: 0; display: flex; align-items: center;
        }
        .profile-thumb {
            width: 44px; height: 44px; border-radius: 50%;
            background-color: #eff6ff; border: 1.5px solid #bfdbfe;
            display: flex; align-items: center; justify-content: center;
            overflow: hidden; flex-shrink: 0;
        }
        .profile-thumb-default {
            font-size: 14px; font-weight: 800; color: var(--blue);
        }
        .profile-dropdown {
            display: none; position: absolute; top: 50px; right: 0;
            min-width: 180px; background: #fff;
            border: 1px solid #e5e7eb; border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,.1); padding: 6px; z-index: 9999;
        }
        .profile-dropdown.open { display: block; }
        .pd-head { padding: 10px 12px 8px; border-bottom: 1px solid #f3f4f8; margin-bottom: 6px; }
        .pd-name { font-size: 14px; font-weight: 800; color: #1c1e26; }
        .pd-item {
            display: block; padding: 10px 12px;
            font-size: 13px; font-weight: 600; color: #4b5563;
            text-decoration: none; border-radius: 8px; transition: background .12s;
        }
        .pd-item:hover { background: #f8f9ff; color: var(--blue); }
        .pd-logout { color: #ef4444; }
        .pd-logout:hover { background: #fff3f3; color: #ef4444; }
        .pd-balance-row {
            display: flex; align-items: center; gap: 6px;
            padding: 10px 12px; margin-bottom: 4px;
            background: #eff6ff; border-radius: 8px;
        }
        .pd-balance-label { font-size: 11px; font-weight: 600; color: #3b82f6; }
        .pd-balance-amount { font-size: 13px; font-weight: 900; color: #1d4ed8; flex: 1; }
        .pd-charge-btn {
            font-size: 11px; font-weight: 700;
            background: var(--blue); color: #fff;
            border-radius: 6px; padding: 3px 9px; text-decoration: none;
        }
        .pd-charge-btn:hover { background: var(--blue-dark); }

        /* 관리자 드롭다운 */
        .admin-dropdown-container { position: relative; display: inline-block; }
        .admin-dropdown-trigger { cursor: pointer; }
        .admin-dropdown-container:hover .nav-submenu { display: flex !important; }
        .nav-submenu {
            display: none; position: absolute; top: 100%; left: 50%; transform: translateX(-50%);
            background: #fff; min-width: 140px;
            border: 1px solid #e5e7eb; border-radius: 6px;
            box-shadow: 0 10px 15px -3px rgba(0,0,0,.1);
            padding: 4px 0; z-index: 9999;
            flex-direction: column;
        }
        .nav-submenu .submenu-link {
            display: block; width: 100%;
            padding: 10px 0; font-size: 13px; font-weight: 600;
            color: #6b7280; text-decoration: none; white-space: nowrap;
            text-align: center; transition: all .1s; position: relative;
        }
        .nav-submenu .submenu-link:hover { color: var(--blue); background: #eff6ff; }
        .nav-submenu .submenu-link.active-link { color: var(--blue); background: #eff6ff; }
        .nav-submenu .submenu-link.active-link::before,
        .nav-submenu .submenu-link:hover::before {
            content: ""; position: absolute; left: 0; top: 0; bottom: 0; width: 4px; background: var(--blue);
        }

        /* 알림 토스트 */
        .noti-toast {
            position: fixed; bottom: 24px; right: 24px;
            background: #eff6ff; border: 1.5px solid #bfdbfe;
            border-radius: 14px; padding: 16px 20px;
            z-index: 9999; animation: notiToastFade 4s ease forwards;
            max-width: 320px; box-shadow: 0 8px 30px rgba(37,99,235,.12);
        }
        .noti-toast-label { font-size: 12px; font-weight: 700; color: var(--blue); margin-bottom: 4px; }
        .noti-toast-content { font-size: 13px; color: #374151; line-height: 1.6; }
        @keyframes notiToastFade {
            0%   { opacity: 0; transform: translateY(10px); }
            15%  { opacity: 1; transform: translateY(0); }
            80%  { opacity: 1; }
            100% { opacity: 0; }
        }

        /* 반응형 */
        @media (max-width: 960px) {
            .nav-inner { padding: 0 16px; }
            .nav-links { display: none; }
            .nav-logo img { height: 24px; }
        }
        @media (max-width: 480px) {
            .nav-inner { padding: 0 12px; }
        }
    </style>
</head>
<body>

<!-- ══ 네브바 ══ -->
<header class="nav">
    <div class="nav-inner">
        {{#isExpert}}
            {{#isAdmin}}
                <a href="/admin/main" class="nav-logo"><img src="/logo-horizontal.svg" alt="IcodeU"></a>
            {{/isAdmin}}
            {{^isAdmin}}
                <a href="/exmain" class="nav-logo"><img src="/logo-horizontal.svg" alt="IcodeU"></a>
            {{/isAdmin}}

            <nav class="nav-links expert-links">
                <a href="/bids/my-list"         class="expert-nav-tab">제안서 관리</a>
                <a href="/bids/my-orders"       class="expert-nav-tab">주문 관리</a>
                <a href="/payment/my"           class="expert-nav-tab">수익 &amp; 정산</a>
                <a href="/reviews/my"           class="expert-nav-tab">평가 및 리뷰</a>
                <a href="/experts/my/info-edit" class="expert-nav-tab">내 정보 수정</a>
                <a href="/boards"               class="expert-nav-tab">게시판</a>
                <a href="/messages"             class="expert-nav-tab">쪽지함</a>
            </nav>

            <div class="nav-actions">
                <div class="nav-balance-wrap">
                    <span class="nav-balance-label">잔액</span>
                    <span class="nav-balance-amount">{{headerBalance}}원</span>
                    <a href="/withdrawal/form" class="nav-charge-btn">출금</a>
                </div>
                <a href="/notifications" class="nav-bell" aria-label="알림">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path>
                        <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
                    </svg>
                    {{#hasNewNotification}}<span class="nav-bell-dot"></span>{{/hasNewNotification}}
                </a>
                <div class="profile-menu" id="profileMenuExpert">
                    <button class="profile-trigger" onclick="toggleProfileMenu(event,'profileDropdownExpert')" aria-label="내 메뉴">
                        {{#sessionUser.profileImage}}
                            <span class="profile-thumb" style="background-image:url('{{sessionUser.profileImageUrl}}');background-size:cover;"></span>
                        {{/sessionUser.profileImage}}
                        {{^sessionUser.profileImage}}
                            <span class="profile-thumb profile-thumb-default">{{sessionUser.name}}</span>
                        {{/sessionUser.profileImage}}
                    </button>
                    <div class="profile-dropdown" id="profileDropdownExpert">
                        <div class="pd-head"><div class="pd-name">{{sessionUser.name}}님</div></div>
                        <a href="/experts/dashboard" class="pd-item">🏠 대시보드</a>
                        <a href="/experts/my/form" class="pd-item">⚙️ 프로필 수정</a>
                        <form action="/logout" method="POST" style="display:inline;">
                            <button type="submit" class="pd-item pd-logout" style="background:none;border:none;width:100%;text-align:left;cursor:pointer;padding:10px 12px;">🚪 로그아웃</button>
                        </form>
                    </div>
                </div>
            </div>
        {{/isExpert}}

        {{^isExpert}}
            {{#isLoggedIn}}
                {{#isAdmin}}
                    <a href="/admin/main" class="nav-logo"><img src="/logo-horizontal.svg" alt="IcodeU"></a>
                {{/isAdmin}}
                {{^isAdmin}}
                    <a href="/exmain" class="nav-logo"><img src="/logo-horizontal.svg" alt="IcodeU"></a>
                {{/isAdmin}}
            {{/isLoggedIn}}
            {{^isLoggedIn}}
                <a href="/" class="nav-logo"><img src="/logo-horizontal.svg" alt="IcodeU"></a>
            {{/isLoggedIn}}

            <nav class="nav-links">
                {{#isAdmin}}
                    <a href="/admin/members" class="admin-main-item">회원 관리</a>
                    <a href="/admin/projects" class="admin-main-item">프로젝트 관리</a>
                    <div class="admin-dropdown-container">
                        <span class="admin-dropdown-trigger">게시판 관리</span>
                        <div class="nav-submenu">
                            <a href="/admin/boards"   class="submenu-link">자유게시판</a>
                            <a href="/admin/notices"  class="submenu-link">공지사항</a>
                            <a href="/admin/reports"  class="submenu-link">댓글신고관리</a>
                            <a href="/admin/inquiry"  class="submenu-link">1:1문의관리</a>
                        </div>
                    </div>
                    <a href="/admin/blacklists" class="admin-main-item">블랙리스트</a>
                    <div class="admin-dropdown-container">
                        <span class="admin-dropdown-trigger">전문가 승인</span>
                        <div class="nav-submenu">
                            <a href="/admin/experts/withdraw"       class="submenu-link">출금 요청 관리</a>
                            <a href="/admin/experts?status=PENDING" class="submenu-link">전문가 승인 관리</a>
                            <a href="/admin/experts/grade"          class="submenu-link">등급별 전문가 관리</a>
                            <a href="/admin/refund-applications"    class="submenu-link">환불 요청 관리</a>
                        </div>
                    </div>
                {{/isAdmin}}
                {{^isAdmin}}
                    {{#isLoggedIn}}
                        <a href="/projects">프로젝트</a>
                        <a href="/experts">전문가</a>
                        <a href="/boards">게시판</a>
                        <a href="/my-pages">마이페이지</a>
                        <a href="/messages">쪽지함</a>
                    {{/isLoggedIn}}
                    {{^isLoggedIn}}
                        <a href="#features"   class="nav-link-intro">서비스 소개</a>
                        <a href="#how"        class="nav-link-intro">이용 방법</a>
                        <a href="#categories" class="nav-link-intro">분야</a>
                        <a href="#trust"      class="nav-link-intro">안전 결제</a>
                        <a href="/projects"   class="nav-link-other" style="display:none;">프로젝트</a>
                        <a href="/experts"    class="nav-link-other" style="display:none;">전문가</a>
                        <a href="/boards"     class="nav-link-other" style="display:none;">게시판</a>
                    {{/isLoggedIn}}
                {{/isAdmin}}
            </nav>

            <div class="nav-actions">
                {{#isLoggedIn}}
                    <a href="/notifications" class="nav-bell" aria-label="알림">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path>
                            <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
                        </svg>
                        {{#hasNewNotification}}<span class="nav-bell-dot"></span>{{/hasNewNotification}}
                    </a>
                    <div class="profile-menu" id="profileMenuUser">
                        <button class="profile-trigger" onclick="toggleProfileMenu(event,'profileDropdownUser')" aria-label="내 메뉴">
                            {{#sessionUser.profileImage}}
                                <span class="profile-thumb" style="background-image:url('{{sessionUser.profileImageUrl}}');background-size:cover;"></span>
                            {{/sessionUser.profileImage}}
                            {{^sessionUser.profileImage}}
                                <span class="profile-thumb profile-thumb-default">{{sessionUser.name}}</span>
                            {{/sessionUser.profileImage}}
                        </button>
                        <div class="profile-dropdown" id="profileDropdownUser">
                            <div class="pd-head"><div class="pd-name">{{sessionUser.name}}님</div></div>
                            {{^isAdmin}}
                                <div class="pd-balance-row">
                                    <span class="pd-balance-label">잔액</span>
                                    <span class="pd-balance-amount">{{headerBalance}}원</span>
                                    <a href="/charge/form" class="pd-charge-btn">충전</a>
                                </div>
                                <a href="/my-pages" class="pd-item">🏠 마이페이지</a>
                                <a href="/my-pages?tab=edit" class="pd-item">⚙️ 정보 수정</a>
                            {{/isAdmin}}
                            <form action="/logout" method="POST" style="display:inline;">
                                <button type="submit" class="pd-item pd-logout" style="background:none;border:none;width:100%;text-align:left;cursor:pointer;padding:10px 12px;">🚪 로그아웃</button>
                            </form>
                        </div>
                    </div>
                {{/isLoggedIn}}
                {{^isLoggedIn}}
                    <a href="/login" class="btn btn-outline">로그인</a>
                    <a href="/join-start" class="btn btn-primary">회원가입</a>
                {{/isLoggedIn}}
            </div>
        {{/isExpert}}
    </div>
</header>

<!-- ══ SSE 알림 (로그인 시) ══ -->
{{#isLoggedIn}}
    <script>
        const sse = new EventSource('/notifications/subscribe');
        sse.addEventListener('notification', function(e) {
            const data = JSON.parse(e.data);
            showNotiToast(data);
            const bell = document.querySelector('.nav-bell');
            if (bell && !bell.querySelector('.nav-bell-dot')) {
                const dot = document.createElement('span');
                dot.className = 'nav-bell-dot';
                bell.appendChild(dot);
            }
        });
        window.addEventListener('beforeunload', function() { sse.close(); });
        function showNotiToast(data) {
            const toast = document.createElement('div');
            toast.className = 'noti-toast';
            toast.innerHTML = '<div class="noti-toast-label">' + data.typeLabel + '</div><div class="noti-toast-content">' + data.content + '</div>';
            document.body.appendChild(toast);
            setTimeout(() => toast.remove(), 4000);
        }
    </script>
{{/isLoggedIn}}

<!-- ══ 공통 스크립트 ══ -->
<script>
    window.addEventListener('DOMContentLoaded', () => {
        /* 비로그인: 인트로(/) 여부에 따라 nav 링크 전환 */
        const isIntroPage = window.location.pathname === '/' || window.location.pathname === '';
        document.querySelectorAll('.nav-link-intro').forEach(el => el.style.display = isIntroPage ? '' : 'none');
        document.querySelectorAll('.nav-link-other').forEach(el => el.style.display = isIntroPage ? 'none' : '');

        /* 프로필 이니셜 */
        document.querySelectorAll('.profile-thumb-default').forEach(el => {
            const name = el.textContent.trim();
            if (name) el.textContent = name.charAt(0);
        });
    });

    function toggleProfileMenu(e, dropdownId) {
        e.stopPropagation();
        document.getElementById(dropdownId).classList.toggle('open');
    }
    document.addEventListener('click', function(e) {
        document.querySelectorAll('.profile-dropdown.open').forEach(function(dd) {
            const menu = dd.closest('.profile-menu');
            if (menu && !menu.contains(e.target)) dd.classList.remove('open');
        });
    });

    /* CSRF 자동 주입 */
    (function() {
        function getCsrfToken() {
            const match = document.cookie.split(';').map(c => c.trim()).find(c => c.startsWith('XSRF-TOKEN='));
            return match ? decodeURIComponent(match.split('=')[1]) : null;
        }
        function addCsrf() {
            const token = getCsrfToken();
            if (!token) return;
            document.querySelectorAll('form').forEach(function(form) {
                const method = (form.method || '').toUpperCase();
                if (['POST','PUT','DELETE','PATCH'].includes(method) && !form.querySelector('input[name="_csrf"]')) {
                    const input = document.createElement('input');
                    input.type = 'hidden'; input.name = '_csrf'; input.value = token;
                    form.appendChild(input);
                }
            });
        }
        if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', addCsrf);
        else addCsrf();
    })();
</script>

<!-- ══ 관리자 활성 메뉴 ══ -->
{{#isAdmin}}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const currentPath = window.location.pathname;
            if (!currentPath.startsWith('/admin')) return;

            document.querySelectorAll('.nav-links > .admin-main-item').forEach(function(link) {
                if (link.getAttribute('href') === currentPath)
                    link.style.cssText = 'color:#2563eb!important;font-weight:bold!important;';
            });

            document.querySelectorAll('.nav-submenu .submenu-link').forEach(function(link) {
                const href = link.getAttribute('href');
                const match =
                    (href.includes('boards')            && currentPath.includes('board')) ||
                    (href.includes('notices')           && currentPath.includes('notice')) ||
                    (href.includes('withdraw')          && currentPath.includes('/admin/experts/withdraw')) ||
                    (href.includes('status=PENDING')    && currentPath.includes('/admin/experts') && !currentPath.includes('withdraw') && !currentPath.includes('grade')) ||
                    (href.includes('grade')             && currentPath.includes('/admin/experts/grade')) ||
                    (href.includes('refund-applications') && currentPath.includes('/admin/refund-applications'));
                if (match) {
                    link.classList.add('active-link');
                    const trigger = link.closest('.admin-dropdown-container')?.querySelector('.admin-dropdown-trigger');
                    if (trigger) trigger.style.cssText = 'color:#2563eb!important;font-weight:bold!important;';
                }
            });
        });
    </script>
{{/isAdmin}}


