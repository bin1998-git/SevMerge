{{> layout/header}}
<style>
    body { background: linear-gradient(135deg, #c8d8f0 0%, #dce8f8 40%, #b8cce8 100%); background-attachment: fixed; }

    .page-wrap {
        min-height: calc(100vh - 64px);
        display: flex; align-items: stretch; justify-content: center;
        padding: 20px 28px 28px; position: relative; z-index: 1;
    }
    .edit-card {
        width: 100%;
        backdrop-filter: blur(30px); -webkit-backdrop-filter: blur(30px);
        background: rgba(255,255,255,.52); border: 1.5px solid rgba(255,255,255,.82);
        border-radius: 28px;
        box-shadow: 0 20px 60px rgba(37,99,235,.1), inset 0 1px 0 rgba(255,255,255,.9);
        display: flex; flex-direction: column; overflow: hidden;
    }
    .edit-card form { flex: 1; display: flex; flex-direction: column; }

    /* 헤더 + 탭 */
    .edit-header {
        display: flex; align-items: center; justify-content: space-between;
        padding: 22px 36px 0; border-bottom: 1px solid rgba(37,99,235,.08); flex-shrink: 0;
    }
    .edit-header-left { display: flex; align-items: center; gap: 14px; }
    .btn-back {
        width: 36px; height: 36px; border-radius: 50%;
        backdrop-filter: blur(8px); background: rgba(255,255,255,.6);
        border: 1.5px solid rgba(255,255,255,.8);
        display: flex; align-items: center; justify-content: center;
        color: #4a6fa5; text-decoration: none; transition: all .15s; flex-shrink: 0;
    }
    .btn-back:hover { background: rgba(255,255,255,.9); border-color: rgba(37,99,235,.3); color: #2563eb; }
    .edit-title { font-size: 20px; font-weight: 900; color: #1e3a6e; letter-spacing: -.5px; }

    /* 탭 */
    .edit-tabs { display: flex; gap: 0; }
    .edit-tab {
        padding: 16px 22px; font-size: 14px; font-weight: 700; color: #4a6fa5;
        border-bottom: 2.5px solid transparent; text-decoration: none; transition: all .15s; white-space: nowrap;
    }
    .edit-tab:hover { color: #1e3a6e; }
    .edit-tab.active { color: #2563eb; border-bottom-color: #2563eb; }

    /* 폼 바디 */
    .edit-body { flex: 1; }
    .section-divider {
        padding: 14px 36px 0; font-size: 11px; font-weight: 700; color: #2563eb;
        letter-spacing: .8px; text-transform: uppercase; border-top: 1px solid rgba(37,99,235,.06);
    }
    .section-divider:first-child { border-top: none; }

    .form-row {
        display: grid; grid-template-columns: 180px 1fr;
        align-items: flex-start; padding: 18px 36px;
        border-bottom: 1px solid rgba(37,99,235,.06); transition: background .15s;
    }
    .form-row:focus-within { background: rgba(37,99,235,.02); }
    .form-row-label { display: flex; align-items: center; gap: 8px; padding-top: 13px; flex-shrink: 0; }
    .form-label-text { font-size: 14px; font-weight: 700; color: #1e3a6e; }
    .form-required { color: #ef4444; font-size: 13px; }

    .form-input {
        width: 100%; padding: 12px 16px; background: rgba(255,255,255,.6);
        border: 1.5px solid rgba(37,99,235,.1); border-radius: 12px;
        font-size: 14px; font-family: inherit; color: #1e3a6e; outline: none; transition: all .18s; box-sizing: border-box;
    }
    .form-input::placeholder { color: #93aecf; }
    .form-input:focus { border-color: #2563eb; background: rgba(255,255,255,.9); box-shadow: 0 0 0 3px rgba(37,99,235,.08); }
    .form-input:disabled { color: #93aecf; background: rgba(243,244,248,.5); cursor: not-allowed; }
    .form-hint { font-size: 12px; color: #4a6fa5; margin-top: 5px; }

    /* 푸터 */
    .edit-footer {
        display: flex; justify-content: flex-end; align-items: center; gap: 10px;
        padding: 18px 36px 24px; border-top: 1px solid rgba(37,99,235,.08); flex-shrink: 0;
    }
    .btn-cancel {
        padding: 11px 24px; border-radius: 20px; font-size: 14px; font-weight: 700;
        backdrop-filter: blur(8px); background: rgba(255,255,255,.65);
        border: 1.5px solid rgba(255,255,255,.85); color: #2d4a7a;
        text-decoration: none; cursor: pointer; font-family: inherit; transition: all .15s;
    }
    .btn-cancel:hover { background: rgba(255,255,255,.9); border-color: rgba(37,99,235,.2); }
    .btn-submit {
        padding: 11px 28px; border-radius: 20px; font-size: 14px; font-weight: 800;
        background: linear-gradient(135deg, #2563eb, #1d4ed8); color: #fff;
        border: none; cursor: pointer; font-family: inherit;
        box-shadow: 0 4px 14px rgba(37,99,235,.3); transition: all .15s;
        display: inline-flex; align-items: center; gap: 6px;
    }
    .btn-submit:hover { transform: translateY(-1px); box-shadow: 0 8px 20px rgba(37,99,235,.4); }

    #pwMatchHint { font-size: 12px; margin-top: 5px; display: block; }

    @media (max-width: 760px) {
        .page-wrap { padding: 12px 12px 16px; }
        .edit-header, .form-row, .section-divider, .edit-footer { padding-left: 18px; padding-right: 18px; }
        .form-row { grid-template-columns: 1fr; gap: 8px; }
        .form-row-label { padding-top: 0; }
    }
</style>

<section class="page-wrap">
    <div class="edit-card">

        <!-- 헤더 + 탭 -->
        <div class="edit-header">
            <div class="edit-header-left">
                <a href="/experts/dashboard" class="btn-back">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M20 11H7.83l5.59-5.59L12 4l-8 8 8 8 1.41-1.41L7.83 13H20v-2z"/></svg>
                </a>
                <div class="edit-title">내 정보 수정</div>
            </div>
            <div class="edit-tabs">
                <a href="/experts/my/info-edit" class="edit-tab active">개인정보 수정</a>
                <a href="/experts/my/form" class="edit-tab">전문가 프로필 수정</a>
            </div>
        </div>

        <!-- 폼 -->
        <form action="/experts/my/info-edit" method="POST">
            <div class="edit-body">

                <div class="section-divider" style="padding-top:18px;">기본 정보</div>

                <div class="form-row">
                    <div class="form-row-label"><span class="form-label-text">이름</span></div>
                    <input type="text" name="name" class="form-input" value="{{member.name}}" placeholder="이름을 입력하세요" maxlength="20"/>
                </div>

                <div class="form-row">
                    <div class="form-row-label"><span class="form-label-text">이메일</span></div>
                    <div>
                        <input type="email" class="form-input" value="{{member.email}}" disabled/>
                        <span class="form-hint">이메일은 변경할 수 없습니다.</span>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-row-label"><span class="form-label-text">전화번호</span></div>
                    <input type="tel" name="phone" class="form-input" value="{{member.phone}}" placeholder="010-0000-0000"/>
                </div>

                <div class="section-divider" style="margin-top:8px;">비밀번호 변경</div>

                <div class="form-row">
                    <div class="form-row-label"><span class="form-label-text">현재 비밀번호</span></div>
                    <input type="password" name="currentPassword" class="form-input" placeholder="현재 비밀번호를 입력하세요"/>
                </div>

                <div class="form-row">
                    <div class="form-row-label"><span class="form-label-text">새 비밀번호</span></div>
                    <div>
                        <input type="password" name="newPassword" id="newPassword" class="form-input" placeholder="새 비밀번호를 입력하세요"/>
                        <span class="form-hint">영문, 숫자 포함 8자 이상 (변경하지 않으려면 비워두세요)</span>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-row-label"><span class="form-label-text">비밀번호 확인</span></div>
                    <div>
                        <input type="password" name="newPasswordConfirm" id="newPasswordConfirm" class="form-input" placeholder="새 비밀번호를 다시 입력하세요"/>
                        <span id="pwMatchHint"></span>
                    </div>
                </div>

            </div>

            <div class="edit-footer">
                <a href="/experts/dashboard" class="btn-cancel">취소</a>
                <button type="submit" class="btn-submit">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/></svg>
                    수정 완료
                </button>
            </div>
        </form>

    </div>
</section>

<script>
    (function () {
        var newPw = document.getElementById('newPassword');
        var confirmPw = document.getElementById('newPasswordConfirm');
        var hint = document.getElementById('pwMatchHint');
        function check() {
            if (!confirmPw.value) { hint.textContent = ''; return; }
            if (newPw.value === confirmPw.value) { hint.textContent = '비밀번호가 일치합니다.'; hint.style.color = '#16a34a'; }
            else { hint.textContent = '비밀번호가 일치하지 않습니다.'; hint.style.color = '#ef4444'; }
        }
        newPw.addEventListener('input', check);
        confirmPw.addEventListener('input', check);
    }());
</script>

{{> layout/footer}}